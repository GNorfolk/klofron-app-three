export function DivIconInfo({ children } : { children: React.ReactNode }) {
  return (
    <div className="flex items-center mt-4 mx-2 text-sm text-gray-500 dark:text-gray-400">
      {children}
    </div>
  )
}
